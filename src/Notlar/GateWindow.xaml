<Window x:Class="Notlar.GateWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework" Style="{StaticResource {x:Type Window}}" Title="Notlar" Width="1000" Height="730" MinWidth="760" MinHeight="600" WindowStyle="None" WindowStartupLocation="CenterScreen">
  <shell:WindowChrome.WindowChrome><shell:WindowChrome CaptionHeight="48" ResizeBorderThickness="6" CornerRadius="12" GlassFrameThickness="0" UseAeroCaptionButtons="False"/></shell:WindowChrome.WindowChrome>
  <Grid>
    <Grid.RowDefinitions><RowDefinition Height="56"/><RowDefinition/><RowDefinition Height="46"/></Grid.RowDefinitions>
    <StackPanel Orientation="Horizontal" Margin="25,0" VerticalAlignment="Center">
      <TextBlock Text="&#xE70B;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Foreground="{DynamicResource Accent}" FontSize="18" Margin="0,0,10,0"/>
      <TextBlock Text="Notlar" FontWeight="SemiBold"/>
    </StackPanel>
    <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" shell:WindowChrome.IsHitTestVisibleInChrome="True">
      <Button Style="{StaticResource IconButton}" Content="&#xE921;" AutomationProperties.Name="Küçült" Click="MinimizeClick"/>
      <Button Style="{StaticResource IconButton}" Content="&#xE922;" AutomationProperties.Name="Büyüt veya geri yükle" Click="MaximizeClick"/>
      <Button Style="{StaticResource IconButton}" Content="&#xE8BB;" AutomationProperties.Name="Kapat" Click="CloseClick"/>
    </StackPanel>
    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
      <StackPanel Width="360" VerticalAlignment="Top" Margin="0,26,0,24">
        <Border Width="66" Height="66" CornerRadius="19" Background="{DynamicResource Selected}" HorizontalAlignment="Left" Margin="0,0,0,24">
          <TextBlock Text="&#xE72E;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Foreground="{DynamicResource Accent}" FontSize="27" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
        <TextBlock x:Name="Heading" Text="Sadece size ait." FontSize="32" FontWeight="SemiBold" Margin="0,0,0,12"/>
        <TextBlock x:Name="Description" TextWrapping="Wrap" Foreground="{DynamicResource Muted}" LineHeight="23" Margin="0,0,0,25"/>
        <StackPanel x:Name="Credentials">
          <TextBlock x:Name="PasswordLabel" Text="Parola" FontSize="12" Foreground="{DynamicResource Muted}" Margin="0,0,0,8"/>
          <PasswordBox x:Name="Password" AutomationProperties.Name="Parola" KeyDown="PasswordKeyDown"/>
          <StackPanel x:Name="ConfirmPanel" Margin="0,15,0,0">
            <TextBlock Text="Parolayı tekrar girin" FontSize="12" Foreground="{DynamicResource Muted}" Margin="0,0,0,8"/>
            <PasswordBox x:Name="ConfirmPassword" AutomationProperties.Name="Parolayı tekrar girin" KeyDown="PasswordKeyDown"/>
          </StackPanel>
          <TextBlock x:Name="MigrationNotice" TextWrapping="Wrap" FontSize="12" LineHeight="19" Foreground="{DynamicResource Muted}" Margin="0,16,0,0" Visibility="Collapsed"/>
          <CheckBox x:Name="UseBackup" Content="Önceki şifreli kaydı aç" Margin="0,18,0,0" Visibility="Collapsed"/>
          <Button x:Name="Submit" Content="Notlarımı koru" Style="{StaticResource Primary}" Margin="0,22,0,0" Click="SubmitClick"/>
          <Button x:Name="Recover" Content="Kurtarma anahtarını kullan" FontSize="12" Foreground="{DynamicResource Muted}" Margin="0,10,0,0" Click="RecoverClick"/>
        </StackPanel>
        <StackPanel x:Name="RecoveryPanel" Visibility="Collapsed">
          <TextBox x:Name="RecoveryCode" IsReadOnly="True" TextWrapping="Wrap" FontFamily="Consolas" FontSize="17" Background="{DynamicResource Surface}" Padding="16" AutomationProperties.Name="Kurtarma anahtarı"/>
          <CheckBox x:Name="Acknowledged" Content="Anahtarı güvenli bir yere kaydettim" Margin="0,22,0,20"/>
          <Button x:Name="Finish" Content="Notlarıma geç" Style="{StaticResource Primary}" Click="FinishClick"/>
        </StackPanel>
        <TextBlock x:Name="Error" TextWrapping="Wrap" Foreground="#E59B86" FontSize="12" LineHeight="19" Margin="0,14,0,0" AutomationProperties.LiveSetting="Polite"/>
      </StackPanel>
    </ScrollViewer>
    <TextBlock Grid.Row="2" Text="Düşünceleriniz burada, güvende." HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="12" Foreground="{DynamicResource Muted}"/>
  </Grid>
</Window>
