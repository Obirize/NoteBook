<Window x:Class="Notlar.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework" xmlns:local="clr-namespace:Notlar" Style="{StaticResource {x:Type Window}}" Title="Notlar" Width="1160" Height="780" MinWidth="800" MinHeight="540" WindowStartupLocation="CenterScreen" WindowStyle="None" ResizeMode="CanResize">
  <shell:WindowChrome.WindowChrome><shell:WindowChrome CaptionHeight="48" ResizeBorderThickness="6" CornerRadius="12" GlassFrameThickness="0" UseAeroCaptionButtons="False"/></shell:WindowChrome.WindowChrome>
  <Border BorderBrush="{DynamicResource Rule}" BorderThickness="1">
    <Grid>
      <Grid.ColumnDefinitions><ColumnDefinition Width="304"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
      <Border Grid.Column="0" Background="{DynamicResource Side}" BorderBrush="{DynamicResource Rule}" BorderThickness="0,0,1,0"/>
      <Grid Grid.Column="0">
        <Grid.RowDefinitions><RowDefinition Height="48"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="60"/></Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="24,0" VerticalAlignment="Center">
          <TextBlock Text="&#xE70B;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Foreground="{DynamicResource Accent}" FontSize="15" Margin="0,0,9,0"/>
          <TextBlock Text="Notlar" FontSize="12" Foreground="{DynamicResource Muted}"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Margin="20,18,20,12">
          <Grid Margin="4,0,0,22">
            <TextBlock x:Name="ListHeading" Text="Notlarım" FontSize="27" FontWeight="SemiBold"/>
            <Button x:Name="NewButton" HorizontalAlignment="Right" Content="&#xE70F;" Style="{StaticResource IconButton}" Foreground="{DynamicResource Accent}" ToolTip="Yeni not (Ctrl+N)" AutomationProperties.Name="Yeni not" Click="NewClick"/>
          </Grid>
          <Border Background="{DynamicResource Surface}" CornerRadius="8" Height="38" Padding="12,0">
            <Grid>
              <Grid.ColumnDefinitions><ColumnDefinition Width="25"/><ColumnDefinition/><ColumnDefinition Width="22"/></Grid.ColumnDefinitions>
              <TextBlock Text="&#xE721;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Foreground="{DynamicResource Muted}" VerticalAlignment="Center" FontSize="13"/>
              <TextBlock x:Name="SearchHint" Grid.Column="1" Text="Notlarda ara" Foreground="{DynamicResource Muted}" VerticalAlignment="Center" FontSize="13" IsHitTestVisible="False"/>
              <TextBox x:Name="SearchInput" Grid.Column="1" VerticalAlignment="Center" FontSize="13" AutomationProperties.Name="Notlarda ara" TextChanged="SearchChanged"/>
              <Button x:Name="ClearSearch" Grid.Column="2" Content="×" Padding="0" Foreground="{DynamicResource Muted}" Visibility="Collapsed" Click="ClearSearchClick" AutomationProperties.Name="Aramayı temizle"/>
            </Grid>
          </Border>
          <Grid Margin="0,16,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
            <Button x:Name="AllFilter" Content="Tüm notlar" FontSize="12" Padding="8,7" Background="{DynamicResource Surface}" Click="AllClick"/>
            <Button x:Name="TrashFilter" Grid.Column="1" Content="Son silinenler" FontSize="12" Padding="8,7" Foreground="{DynamicResource Muted}" Click="TrashClick"/>
          </Grid>
          <DockPanel Margin="2,10,0,0">
            <Button x:Name="SelectButton" Content="Seç" FontSize="12" Padding="8,4" DockPanel.Dock="Right" Click="SelectModeClick"/>
            <Button x:Name="SelectAllButton" Content="Tümünü seç" FontSize="12" Padding="8,4" DockPanel.Dock="Right" Click="SelectAllClick" Visibility="Collapsed"/>
            <TextBlock x:Name="SelectionCount" FontSize="11" Foreground="{DynamicResource Muted}" VerticalAlignment="Center"/>
          </DockPanel>
          <TextBlock x:Name="TrashNotice" Text="Silinen notlar 30 gün sonra kalıcı silinir." FontSize="11" Foreground="{DynamicResource Muted}" Margin="4,8,0,0" Visibility="Collapsed" TextWrapping="Wrap"/>
        </StackPanel>
        <Grid Grid.Row="2">
          <ListBox x:Name="NoteList" Margin="12,0,12,0" BorderThickness="0" Background="Transparent" Foreground="{DynamicResource Ink}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.CanContentScroll="False" local:SmoothScroll.Scale="0.65" SelectionMode="Single" SelectionChanged="SelectionChanged" ContextMenuOpening="ListMenuOpening" AutomationProperties.Name="Not listesi">
            <ListBox.ContextMenu><ContextMenu>
              <MenuItem x:Name="MenuPin" Header="Sabitle" Click="PinClick"/>
              <MenuItem x:Name="MenuExport" Header="TXT olarak dışa aktar…" InputGestureText="Ctrl+Shift+S" Click="ExportClick"/>
              <MenuItem x:Name="MenuDelete" Header="Son silinenlere taşı" InputGestureText="Delete" Click="DeleteClick"/>
              <MenuItem x:Name="MenuRestore" Header="Geri yükle" Click="RestoreClick"/>
              <MenuItem x:Name="MenuPurge" Header="Kalıcı sil…" Style="{StaticResource DangerMenuItem}" Click="PurgeClick"/>
            </ContextMenu></ListBox.ContextMenu>
            <ListBox.ItemContainerStyle><Style TargetType="ListBoxItem">
              <Setter Property="HorizontalContentAlignment" Value="Stretch"/><Setter Property="Padding" Value="0"/>
              <Setter Property="Margin" Value="0,3"/><Setter Property="Background" Value="Transparent"/>
              <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
              <Setter Property="AutomationProperties.Name" Value="{Binding DisplayTitle}"/>
              <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ListBoxItem">
                <Border x:Name="Card" CornerRadius="9" Background="{TemplateBinding Background}" Padding="16,13"><ContentPresenter/></Border>
                <ControlTemplate.Triggers>
                  <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Card" Property="Background" Value="{DynamicResource Surface}"/></Trigger>
                  <Trigger Property="IsSelected" Value="True"><Setter TargetName="Card" Property="Background" Value="{DynamicResource Selected}"/></Trigger>
                  <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Card" Property="BorderBrush" Value="{DynamicResource Accent}"/><Setter TargetName="Card" Property="BorderThickness" Value="1"/></Trigger>
                </ControlTemplate.Triggers>
              </ControlTemplate></Setter.Value></Setter>
            </Style></ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate><DataTemplate>
              <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/></Grid.ColumnDefinitions>
                <CheckBox VerticalAlignment="Top" Margin="0,1,12,0" AutomationProperties.Name="Notu seç" IsChecked="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}, Mode=TwoWay}">
                  <CheckBox.Style><Style TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding SelectionMode, RelativeSource={RelativeSource AncestorType=ListBox}}" Value="Multiple"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></CheckBox.Style>
                </CheckBox>
                <StackPanel Grid.Column="1">
                  <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <TextBlock Text="{Binding DisplayTitle}" FontWeight="SemiBold" FontSize="14" TextTrimming="CharacterEllipsis"/>
                    <TextBlock Grid.Column="1" Text="&#xE718;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Foreground="{DynamicResource Accent}" FontSize="11" Margin="8,3,0,0"><TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding Pinned}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></TextBlock.Style></TextBlock>
                  </Grid>
                  <TextBlock Text="{Binding Preview}" Foreground="{DynamicResource Muted}" FontSize="12" TextTrimming="CharacterEllipsis" Margin="0,6,0,8"/>
                  <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding DateLabel}" Foreground="{DynamicResource Muted}" FontSize="11"/>
                    <TextBlock Text="{Binding TrashLabel}" Foreground="{DynamicResource Muted}" FontSize="11" Margin="10,0,0,0" TextTrimming="CharacterEllipsis"><TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding Deleted}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></TextBlock.Style></TextBlock>
                  </StackPanel>
                </StackPanel>
              </Grid>
            </DataTemplate></ListBox.ItemTemplate>
          </ListBox>
          <TextBlock x:Name="NoResults" Text="Aramanızla eşleşen not yok." Visibility="Collapsed" TextWrapping="Wrap" Foreground="{DynamicResource Muted}" FontSize="13" Margin="28,28" HorizontalAlignment="Center"/>
        </Grid>
        <Border Grid.Row="3" BorderBrush="{DynamicResource Rule}" BorderThickness="0,1,0,0" Margin="20,10,20,0">
          <Grid>
            <TextBlock x:Name="CountText" Text="0 not" FontSize="11" Foreground="{DynamicResource Muted}" VerticalAlignment="Center" Margin="4,0,0,0"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
              <Button x:Name="ImportButton" Content="&#xE8E5;" Style="{StaticResource IconButton}" ToolTip="TXT dosyası aç (Ctrl+O)" AutomationProperties.Name="TXT dosyası aç" Click="ImportClick"/>
              <Button Content="&#xE74E;" Style="{StaticResource IconButton}" ToolTip="Şifreli yedek al" AutomationProperties.Name="Şifreli yedek al" Click="BackupClick"/>
              <Button x:Name="LockButton" Content="&#xE72E;" Style="{StaticResource IconButton}" ToolTip="Kilitle (Ctrl+L)" AutomationProperties.Name="Kilitle" Click="LockClick"/>
            </StackPanel>
          </Grid>
        </Border>
      </Grid>
      <Grid Grid.Column="1">
        <Grid.RowDefinitions><RowDefinition Height="48"/><RowDefinition/><RowDefinition Height="40"/></Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="16,0,8,0">
          <StackPanel x:Name="NoteActions" Orientation="Horizontal" VerticalAlignment="Center" shell:WindowChrome.IsHitTestVisibleInChrome="True">
            <Button x:Name="PinButton" Content="&#xE718;" Style="{StaticResource IconButton}" ToolTip="Notu sabitle" AutomationProperties.Name="Notu sabitle" Click="PinClick"/>
            <Button x:Name="DeleteButton" Content="&#xE74D;" Style="{StaticResource IconButton}" ToolTip="Son silinenlere taşı" AutomationProperties.Name="Notu sil" Click="DeleteClick"/>
            <Button x:Name="RestoreButton" Content="Geri yükle" FontSize="12" Foreground="{DynamicResource Accent}" Visibility="Collapsed" Click="RestoreClick"/>
            <Button x:Name="PurgeButton" Content="Kalıcı sil" FontSize="12" Foreground="{DynamicResource Danger}" Visibility="Collapsed" ToolTip="Notu geri alınamaz şekilde sil" Click="PurgeClick"/>
            <Button x:Name="ExportButton" Content="&#xE898;" Style="{StaticResource IconButton}" ToolTip="TXT olarak dışa aktar — şifresiz kopya (Ctrl+Shift+S)" AutomationProperties.Name="TXT olarak dışa aktar" Click="ExportClick"/>
          </StackPanel>
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" shell:WindowChrome.IsHitTestVisibleInChrome="True">
            <Button Content="&#xE921;" Style="{StaticResource IconButton}" Focusable="False" AutomationProperties.Name="Küçült" Click="MinimizeClick"/>
            <Button Content="&#xE922;" Style="{StaticResource IconButton}" Focusable="False" AutomationProperties.Name="Büyüt veya geri yükle" Click="MaximizeClick"/>
            <Button Content="&#xE8BB;" Style="{StaticResource IconButton}" Focusable="False" AutomationProperties.Name="Kapat" Click="CloseClick"/>
          </StackPanel>
        </Grid>
        <Grid Grid.Row="1" x:Name="EditorArea" Margin="48,14,12,0">
          <Grid.RowDefinitions><RowDefinition Height="38"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
          <TextBlock x:Name="DateText" HorizontalAlignment="Center" FontSize="11" Foreground="{DynamicResource Muted}"/>
          <Grid Grid.Row="1" Margin="0,12,0,22">
            <TextBlock x:Name="TitleHint" Text="Yeni not" FontSize="30" FontWeight="SemiBold" Foreground="{DynamicResource Muted}" IsHitTestVisible="False"/>
            <TextBox x:Name="TitleInput" Margin="0,0,36,0" FontSize="30" FontWeight="SemiBold" MaxLength="500" AutomationProperties.Name="Not başlığı" TextChanged="EditorChanged" KeyDown="TitleKeyDown"/>
          </Grid>
          <Grid Grid.Row="2">
            <TextBlock x:Name="BodyHint" Margin="0,0,36,0" Text="Aklınızdakileri yazın…" FontSize="16" Foreground="{DynamicResource Muted}" IsHitTestVisible="False"/>
            <TextBox x:Name="BodyInput" Padding="0,0,36,0" FontSize="16" TextBlock.LineHeight="28" AcceptsReturn="True" AcceptsTab="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" IsUndoEnabled="True" UndoLimit="100" SpellCheck.IsEnabled="False" AutomationProperties.Name="Not içeriği" TextChanged="EditorChanged"/>
          </Grid>
        </Grid>
        <StackPanel x:Name="EmptyState" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="370" Margin="30">
          <TextBlock Text="&#xE70B;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Foreground="{DynamicResource Accent}" FontSize="38" HorizontalAlignment="Center" Margin="0,0,0,24"/>
          <TextBlock x:Name="EmptyHeading" Text="Bir düşünceyle başlar." FontSize="25" FontWeight="SemiBold" TextAlignment="Center"/>
          <TextBlock x:Name="EmptyDescription" Text="Küçük bir fikir, uzun bir gün, unutmamak istediğiniz bir şey." FontSize="14" Foreground="{DynamicResource Muted}" TextWrapping="Wrap" TextAlignment="Center" LineHeight="23" Margin="0,12,0,24"/>
          <Button x:Name="EmptyNew" Content="İlk notunuzu yazın" Style="{StaticResource Primary}" HorizontalAlignment="Center" Click="NewClick"/>
          <TextBlock Text="Ctrl + N" FontSize="11" Foreground="{DynamicResource Muted}" HorizontalAlignment="Center" Margin="0,12,0,0"/>
        </StackPanel>
        <StackPanel x:Name="SelectionState" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="400" Margin="30" Visibility="Collapsed">
          <TextBlock Text="&#xE762;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Foreground="{DynamicResource Accent}" FontSize="38" HorizontalAlignment="Center" Margin="0,0,0,24"/>
          <TextBlock x:Name="SelectionHeading" Text="Not seçin" FontSize="25" FontWeight="SemiBold" TextAlignment="Center"/>
          <TextBlock x:Name="SelectionDescription" Text="" FontSize="14" Foreground="{DynamicResource Muted}" TextWrapping="Wrap" TextAlignment="Center" LineHeight="23" Margin="0,12,0,24"/>
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="BulkDeleteButton" Content="Son silinenlere taşı" Style="{StaticResource Primary}" Click="DeleteClick"/>
            <Button x:Name="BulkRestoreButton" Content="Geri yükle" Style="{StaticResource Primary}" Visibility="Collapsed" Click="RestoreClick"/>
            <Button x:Name="BulkPurgeButton" Content="Kalıcı sil" Foreground="{DynamicResource Danger}" Padding="20,13" Margin="8,0,0,0" Visibility="Collapsed" Click="PurgeClick"/>
          </StackPanel>
          <TextBlock Text="Ctrl + A tümünü seçer" FontSize="11" Foreground="{DynamicResource Muted}" HorizontalAlignment="Center" Margin="0,12,0,0"/>
        </StackPanel>
        <Grid Grid.Row="2" Margin="26,0">
          <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock x:Name="StatusText" Text="Şifreli olarak kaydedildi" Foreground="{DynamicResource Muted}" FontSize="11" AutomationProperties.LiveSetting="Polite"/>
            <Button x:Name="UndoDelete" Content="Geri al" Foreground="{DynamicResource Accent}" FontSize="11" Padding="8,3" Margin="8,0,0,0" Visibility="Collapsed" Click="UndoDeleteClick"/>
          </StackPanel>
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
            <Button x:Name="UpdateButton" Content="Yeni sürüm hazır · Güncelle" Foreground="{DynamicResource Accent}" FontSize="11" Padding="8,3" Margin="0,0,10,0" Visibility="Collapsed" Click="UpdateClick"/>
            <TextBlock x:Name="VersionText" Text="Bu bilgisayarda" Foreground="{DynamicResource Muted}" FontSize="11" VerticalAlignment="Center"/>
          </StackPanel>
        </Grid>
      </Grid>
    </Grid>
  </Border>
</Window>
