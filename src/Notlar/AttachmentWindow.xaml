<Window x:Class="Notlar.AttachmentWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework" xmlns:local="clr-namespace:Notlar" Style="{StaticResource {x:Type Window}}" Width="1000" Height="720" MinWidth="480" MinHeight="360" WindowStartupLocation="CenterOwner" WindowStyle="None" ResizeMode="CanResize" ShowInTaskbar="False">
  <shell:WindowChrome.WindowChrome><shell:WindowChrome CaptionHeight="48" ResizeBorderThickness="6" CornerRadius="12" GlassFrameThickness="0" UseAeroCaptionButtons="False"/></shell:WindowChrome.WindowChrome>
  <Border BorderBrush="{DynamicResource Rule}" BorderThickness="1" Background="#151516">
    <Grid>
      <Grid.RowDefinitions><RowDefinition Height="48"/><RowDefinition/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
      <Grid Margin="20,0,8,0">
        <StackPanel Orientation="Vertical" VerticalAlignment="Center" Margin="0,0,200,0">
          <TextBlock x:Name="NameText" FontSize="13" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
          <TextBlock x:Name="DetailText" FontSize="11" Foreground="{DynamicResource Muted}" TextTrimming="CharacterEllipsis"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" shell:WindowChrome.IsHitTestVisibleInChrome="True">
          <Button x:Name="SaveButton" Content="&#xE792;" Style="{StaticResource IconButton}" ToolTip="{local:T AttachmentSaveAs}" AutomationProperties.Name="{local:T AttachmentSaveAs}" Click="SaveClick"/>
          <Button Content="&#xE8BB;" Style="{StaticResource IconButton}" Focusable="False" AutomationProperties.Name="{local:T Close}" Click="CloseClick"/>
        </StackPanel>
      </Grid>
      <Grid Grid.Row="1" Margin="12,0,12,12">
        <Image x:Name="Picture" Stretch="Uniform" StretchDirection="DownOnly" RenderOptions.BitmapScalingMode="HighQuality" Visibility="Collapsed"/>
        <MediaElement x:Name="Player" LoadedBehavior="Manual" UnloadedBehavior="Stop" Stretch="Uniform" Visibility="Collapsed" MediaOpened="MediaOpened" MediaEnded="MediaEnded" MediaFailed="MediaFailed" MouseLeftButtonUp="TogglePlay"/>
        <TextBlock x:Name="ErrorText" Text="{local:T AttachmentOpenFailed}" Foreground="{DynamicResource Muted}" TextWrapping="Wrap" TextAlignment="Center" MaxWidth="420" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
      </Grid>
      <Grid Grid.Row="2" x:Name="Controls" Margin="20,0,20,14" Visibility="Collapsed">
        <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
        <Button x:Name="PlayButton" Content="&#xE769;" Style="{StaticResource IconButton}" ToolTip="{local:T PlayPause}" AutomationProperties.Name="{local:T PlayPause}" Click="TogglePlay"/>
        <Slider x:Name="Seek" Grid.Column="1" VerticalAlignment="Center" Margin="12,0" Minimum="0" Maximum="1" IsMoveToPointEnabled="True" Thumb.DragStarted="SeekStarted" Thumb.DragCompleted="SeekCompleted" ValueChanged="SeekChanged"/>
        <TextBlock x:Name="TimeText" Grid.Column="2" FontSize="11" Foreground="{DynamicResource Muted}" VerticalAlignment="Center" MinWidth="80" TextAlignment="Right"/>
      </Grid>
    </Grid>
  </Border>
</Window>
